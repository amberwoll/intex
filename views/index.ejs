<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Turtle Shelter Project</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: url('../pictures/homepage.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-around;
            min-height: 100vh;
            color: white;
        }

        .top-right {
            position: absolute;
            top: 20px;
            right: 20px;
        }

        .top-right button {
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .top-right button:hover {
            background-color: rgba(255, 255, 255, 0.7);
            color: black;
        }

        .bottom-buttons {
            display: flex;
            justify-content: center;
            width: 100%;
            margin-top: 20px;
        }

        .bottom-buttons form {
            margin: 0 10px;
        }

        .bottom-buttons button {
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .bottom-buttons button:hover {
            background-color: rgba(255, 255, 255, 0.7);
            color: black;
        }

        .new-section {
            width: 80%;
            padding: 40px;
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 10px;
            text-align: center;
            margin-bottom: 20px;
        }

        .new-section h1 {
            font-size: 36px;
            margin-bottom: 20px;
        }

        .new-section p {
            font-size: 20px;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="top-right">
        <form action="/admin_login" method="GET">
            <button type="submit">Admin Login</button>
        </form>
    </div>
    <div class="new-section">
        <p>Helping to Save Lives of People Experiencing Homelessness</p>
        <h1>Starts with You!</h1>
    </div>
    <div class="bottom-buttons">
        <form action="/application" method="GET">
            <button type="submit">Volunteer Application</button>
        </form>
        <form action="/host" method="GET">
            <button type="submit">Hosting An Event With Us</button>
        </form>
        <form action="https://turtleshelterproject.org/donate" method="GET">
            <button type="submit">Donate</button>
        </form>
    </div>
</body>
</html>